"""Project configuration: paths, calendars, events, factor blocks, chain taxonomy.

Everything mutable is in YAML; only immutable Python paths live in ``paths.py``.
"""

from config.paths import *  # noqa: F401,F403
