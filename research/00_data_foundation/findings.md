# Findings

- The local raw universe has 584 files across 8 provider groups.
- 7 provider groups are not classified as public/re-distributable, so public artifacts must remain derived outputs unless rights are separately verified.
- The configured registry contains 31 features; processed panels expose 796 panel-column entries across local parquet artifacts.
- The usage matrix assigns 25 primary-analysis, 69 diagnostic-only, and 103 excluded statuses.
- The canonical identity audit has 0 non-pass checks.
- Automated data-quality review emitted 153 flags. Severity and required action are in `tables/data_quality_flags.csv`.
