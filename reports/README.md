# Reports

This folder now contains compatibility outputs used by legacy scripts:

- `figures/`: dated generated figure bundles.
- `tables/`: dated generated table bundles.
- `panels/`: master panel metadata and ignored parquet panel artifacts.

The public artifact packet is [`../outputs/`](../outputs/). Historical release
packets and internal process docs were moved to [`../archive/`](../archive/).
