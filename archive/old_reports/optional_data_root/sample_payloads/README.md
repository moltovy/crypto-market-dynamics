# Static Payload Samples

This directory is reserved for small manually curated sample payloads if future
tests need fixture files. The current tests keep payloads inline and do not make
network requests.
